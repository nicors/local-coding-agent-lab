# Cenários de baseline

Crie um arquivo por cenário, identificando modelo, quantização, versão do `llama.cpp`, parâmetros do servidor, hardware e versão do OpenCode. Não compare resultados que usem tarefas ou estados iniciais diferentes.
